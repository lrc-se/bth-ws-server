{
  "name": "ws-server",
  "version": "1.0.0",
  "description": "Simple Web Sockets server",
  "main": "index.js",
  "files": [
    "index.js",
    "src"
  ],
  "scripts": {
    "test": "npm-run-all -c lint tap",
    "tap": "tap -c --reporter=spec --coverage --coverage-report=html --no-browser test/*.js",
    "tap-clover": "tap --reporter=tap --coverage --coverage-report=clover test/*.js",
    "lint": "eslint ."
  },
  "keywords": [
    "WebSocket",
    "WebSockets"
  ],
  "homepage": "https://github.com/lrc-se/bth-ws-server",
  "bugs": "https://github.com/lrc-se/bth-ws-server/issues",
  "repository": "lrc-se/bth-ws-server",
  "author": "LRC <kabc16@student.bth.se>",
  "license": "MIT",
  "dependencies": {
    "ws": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^4.12.1",
    "eslint-plugin-react": "^7.5.1",
    "npm-run-all": "^4.1.2",
    "tap": "^11.0.0"
  }
}
